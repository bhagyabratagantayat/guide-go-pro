---
wave: 1
depends_on: []
files_modified: ["frontend/src/config.js", "frontend/src/pages/BookingPage.jsx", "frontend/src/pages/BookingStatusPage.jsx", "frontend/src/pages/ExplorePage.jsx", "frontend/src/pages/GuideListPage.jsx", "frontend/src/pages/PlaceDetailsPage.jsx", "frontend/src/pages/guidePortal/GuideBookings.jsx", "frontend/src/pages/guidePortal/GuideDashboard.jsx", "frontend/src/context/SocketContext.jsx"]
autonomous: true
requirements_addressed: ["SYS-01", "SYS-03"]
---

# Phase 7: Polish & Final QA

<objective>
To finalize the platform for production readiness. We must strip all localized `localhost:3000` domains across the Vite build schema to allow Cloud deployments (Vercel/Render) to handshake seamlessly. Finally, we must augment the Booking interface with the required glassmorphism Tailwind tags mirroring UX directives.
</objective>

<context>
The frontend application was developed utilizing direct `axios.get('http://localhost:3000')` requests hardcoded into component `useEffect` boundaries. This blocks Vercel deployment mappings. Additionally, Socket.io instantiation ignores global configuration logic. This phase rewrites these strings to utilize `import.meta.env.VITE_API_URL` dynamically.
</context>

<task>
<read_first>
- `frontend/src/config.js`
- `frontend/src/context/SocketContext.jsx`
- `frontend/src/pages/BookingPage.jsx`
</read_first>
<action>
1. Modify `frontend/src/config.js` exporting a `getBaseUrl()` utility mapping directly into `import.meta.env.VITE_API_URL || 'http://localhost:3000'`.
2. Target `SocketContext.jsx` to pull connection strings explicitly from `getBaseUrl()`.
3. Use global Javascript string substitutions to sweep `/pages` modifying local `axios` invocations to parse standard Configuration prefixes.
4. Inject `backdrop-blur-xl bg-white/40 border-white/20` across `BookingPage.jsx` to finalize Glass UI assignments matching core directives.
5. Create production `.env.production` in frontend scoping the real production values.
</action>
<acceptance_criteria>
- Zero `localhost:3000` hardcodes exist natively across `.jsx` frontend files.
- `SocketContext` operates conditionally allowing production routing.
- Glassmorphism verified over standard `bg-` tags.
</acceptance_criteria>
</task>

<verification>
## Verification Criteria
- [ ] Run `grep http://localhost:3000` inside `frontend/src` explicitly checking for zero matching lines!
</verification>
