---
wave: 1
depends_on: []
files_modified: ["backend/.env.example", "backend/middleware/authMiddleware.js"]
autonomous: true
requirements_addressed: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04", "SYS-02"]
---

# Phase 1: Database & Core Auth

<objective>
Verify and enforce the existing MongoDB integration, complete environment variable scaffolding, and ensure the robust JWT role-based authentication properly gates unverified Guides and unauthorized Users.
</objective>

<context>
The Guide Go backend already contains models for `User.js` and `Guide.js` as well as a pre-populated `authController.js`. Phase 1 execution focuses on ensuring these mechanisms work flawlessly end-to-end and the `.env` configuration is strictly secured.
</context>

<task>
<read_first>
- `backend/server.js`
- `backend/controllers/authController.js`
- `backend/middleware/authMiddleware.js` (needs to be checked for role checking)
</read_first>
<action>
1. Check `backend/.env` for missing setup (PORT=5000, MONGO_URI, JWT_SECRET, Cloudinary configs). Create `backend/.env.example` mirroring it.
2. Verify `authMiddleware.js` contains the `protect` function and a `authorizeRoles(...roles)` function to enforce role-based routing (Admin vs User).
3. Verify that logging in as an unverified guide throws the appropriate 401 error. (This logic exists in `authController.js` line 83 `if (!guide || !guide.isVerified)`).
4. Create a formal test or manually send a request via run_command to verify these auth endpoints.
</action>
<acceptance_criteria>
- `backend/.env.example` exists and contains `PORT=5000`, `MONGO_URI=`, `JWT_SECRET=`, `CLOUDINARY_CLOUD_NAME=`, `CLOUDINARY_API_KEY=`, `CLOUDINARY_API_SECRET=`.
- `backend/middleware/authMiddleware.js` contains an `authorizeRoles` or equivalent Admin blocker logic.
- Node server boot-up successfully connects to MongoDB Atlas.
</acceptance_criteria>
</task>

<verification>
## Verification Criteria
- [ ] User, Guide, and Admin can successfully log in and receive a JWT.
- [ ] Unverified guides cannot bypass the login block.
- [ ] Access to an Admin-only protected route returns 401/403 for Users.

## Must Haves
- MongoDB connection succeeds
- Guide login verification block functions properly
- Role middleware guards routes
</verification>
